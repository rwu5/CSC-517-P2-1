<%= form_with(model: library_book_list, local: true) do |form| %>
  <% if library_book_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(library_book_list.errors.count, "error") %> prohibited this library_book_list from being
        saved:</h2>

      <ul>
        <% library_book_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <% if !current_librarian %>
    <div class="field">
      <%= form.label :book_id %>
      <%= form.text_field :book_id %>
      <p>Please type Book ID</p>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
