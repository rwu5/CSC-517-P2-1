<%= form_with(model: student_current_borrow_list, local: true) do |form| %>
  <% if student_current_borrow_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_current_borrow_list.errors.count, "error") %> prohibited this student_current_borrow_list from being saved:</h2>

      <ul>
        <% student_current_borrow_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
